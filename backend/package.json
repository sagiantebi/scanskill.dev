{
  "name": "skills-scanner-backend",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787 -c wrangler.jsonc -c ../workers/worker1/wrangler.jsonc -c ../workers/worker2/wrangler.jsonc -c ../workers/worker3/wrangler.jsonc",
    "deploy": "npm run build --prefix ../frontend && wrangler deploy",
    "deploy:staging": "npm run build --prefix ../frontend && wrangler deploy --env staging",
    "deploy:production": "npm run build --prefix ../frontend && wrangler deploy --env production",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "test:pure": "vitest run --config vitest.pure.config.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply DB --config wrangler.jsonc --local",
    "db:migrate:staging": "wrangler d1 migrations apply DB --config wrangler.jsonc --env staging --remote",
    "db:migrate:production": "wrangler d1 migrations apply DB --config wrangler.jsonc --env production --remote",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@hono/zod-validator": "^0.7.6",
    "drizzle-orm": "^0.45.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@cloudflare/workers-types": "^4.20250320.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1",
    "wrangler": "^4.0.0"
  }
}